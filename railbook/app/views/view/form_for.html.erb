<%= form_for(@book,url: books_path, html: {id: :new_book, class: :new_book}) do |f| %>
  <div class="field">
    <%= f.label :isbn %><br />
    <%= f.text_field :isbn, size: 25 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
